meta {
  name: Criar tenant
  type: http
  seq: 1
}

post {
  url: {{base_url}}/admin/tenants
  body: json
  auth: bearer
}

auth:bearer {
  token: {{admin_token}}
}

body:json {
  {
    "name": "IBM Telco",
    "slug": "ibm-telco"
  }
}
